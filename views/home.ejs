
<%- include("partials/header"); -%>

<h1>HOME</h1>
<p><%= startingCont %> </p>

  <% newPost.forEach(function(element){ %>
        <h1><%= element.title %> </h1>
        
        <p>
            <%= element.content.substring(0, 100).concat('...') %>
            <a href="/new/<%= element._id %> ">Read More</a>
        </p>
        
  <% }); %>

<%- include("partials/footer"); -%>